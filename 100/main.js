function verificarNumero() {

    var numeroRecebido = document.getElementById("valornumero")
    var numeroCorreto = 100
    
    if (numberRecebido != numeroCorreto) {
        alert("Digite um novo número");
    } else {
        alert("Acertou mizeravi!");
    }
}
